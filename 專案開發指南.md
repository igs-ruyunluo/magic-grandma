# 魔法阿嬤（Magic Grandma）— 專案開發指南

> 這份文件的目的：讓任何人（包括編輯器內建的 AI 助手）接手這個專案時，能快速理解遊戲設計、技術架構、目前進度、以及接下來該做什麼。

---

## 一、遊戲概念

**類型**：Crash Game（原型參考：inout.games 這類「下注 → 倍率持續上升 → 玩家自選時機停止結算 → 隨機時刻崩潰」的遊戲）

**主題包裝**：巫婆煉製藥水
- 玩家下注，巫婆開始煉藥
- 倍率隨時間持續上升，代表藥水品質累積
- 玩家可以隨時選擇「停止煉製，把藥賣掉」→ 拿走目前倍率 × 下注金額
- 但每一刻都有崩潰風險（藥水爆炸）→ 崩潰後玩家輸掉全部下注
- 崩潰的時間點，在遊戲開始的瞬間就已經用機率公式決定好了，只是玩家看不到

---

## 二、技術棧

| 技術 | 角色 | 選擇原因 |
|---|---|---|
| **falcon** | Python web 框架，定義 API 路由 | 公司內部技術棧指定，輕量、高效能 |
| **gevent** | 協程（coroutine）併發處理 | 讓單一 worker 能同時服務大量玩家的 WebSocket 連線，不用每個連線各開一個 thread |
| **gunicorn** | WSGI HTTP Server，正式環境運行應用程式的伺服器 | 支援多 worker、自動重啟、production-grade |
| **gevent-websocket** | 讓 gevent + falcon 支援 WebSocket 升級 | 遊戲需要即時推送倍率，一般 HTTP 做不到 |
| **WSGI** | Python web 應用程式的標準介面規範 | falcon 跟 gunicorn 能互相搭配運作的共同「插頭規格」 |

**核心原理筆記，已經整理過的部分**（如果需要複習細節，回頭查這些檔案）：
- Python JSON 筆記.md
- TCPIP四層通訊協定筆記.md
- HTTP協定筆記.md
- HTTP_Keepalive筆記.md
- WSGI與requests筆記.md
- WebSocket筆記.md

---

## 三、遊戲核心機率設計（已完成，經過驗證）

### 崩潰點（Crash Point）公式

```python
import random

def generate_crash_point(house_edge=0.02):
    """
    house_edge: 莊家優勢，例如 0.02 代表 2%，對應 RTP（玩家回報率）約 98%
    回傳這一局會在哪個倍率崩潰（一開始就算好，玩家看不到，只用來跟即時倍率比較）
    """
    r = random.random()
    if r == 0:
        r = 0.0000001  # 避免除以零
    crash_point = (1.00 - house_edge) / r
    return max(crash_point, 1)
```

**設計原理**：`1/r` 這種形式，`r` 是均勻分佈的隨機數，但取倒數後會產生**長尾分佈**——大部分結果集中在低倍率（1.0x~2.0x），但偶爾出現極端高倍率，這是這類遊戲讓人持續下注的核心心理機制。完整推導過程見《巫婆煉藥遊戲_機率設計筆記.md》。

**驗證方式**：用蒙地卡羅模擬（跑幾萬次，統計平均回報率），確認實際 RTP 跟預期一致，不能只憑感覺調參數。

### 倍率成長函式

```python
def grow_multiplier(current_multi, growth_rate=1.03):
    """
    每呼叫一次，代表時間往前推進一步，倍率往上成長一次
    growth_rate 決定遊戲節奏：越接近 1，遊戲越慢、越有張力；越大，遊戲越快結束
    """
    new_multi = current_multi * growth_rate
    return new_multi
```

**已驗證**：`growth_rate=1.3` 會讓遊戲在極短時間內衝到高倍率（節奏過快，玩家來不及反應），`growth_rate=1.03` 左右的節奏比較接近參考遊戲的體感。**最終數值建議用模擬跑出「平均每局幾秒」，反推出適合的 `growth_rate`，不要憑感覺定。**

### 單局遊戲邏輯（已完成，邏輯驗證過）

```python
def run_game_round():
    crash_point = generate_crash_point()
    current_multi = 1.00

    while current_multi < crash_point:
        current_multi = grow_multiplier(current_multi)

    return crash_point, current_multi
```

**重要細節**：迴圈結束時 `current_multi` 會「超過」`crash_point`（因為是先檢查條件才成長），真正要顯示給玩家的「爆炸倍率」是迴圈結束後的 `current_multi`，不是迴圈內最後一次的中間值。

---

## 四、專案檔案架構

```
witch_brew_game/
│
├── app.py                          # 程式進入點，建立 falcon app，組裝所有路由（尚未寫）
├── config.py                       # 設定值集中管理（house_edge、growth_rate 等）（尚未寫）
│
├── game/                           # 遊戲核心邏輯（跟框架無關的純邏輯）
│   ├── __init__.py                 # 已建立（空檔案）
│   ├── crash.py                    # 已完成：generate_crash_point()
│   ├── multiplier.py               # 已完成：grow_multiplier()
│   └── room.py                     # 已完成：run_game_round()（目前是「一次跑完才回傳」的版本，還沒有逐步推送）
│
├── resources/                      # falcon 的 API 端點定義（HTTP + WebSocket）
│   ├── __init__.py                 # 已建立（空檔案）
│   ├── auth.py                     # 登入/註冊相關 API（尚未寫，晚點做）
│   ├── bet.py                      # 下注 API（HTTP POST）（尚未寫）
│   ├── game_ws.py                  # 進行中：目前只有最簡單的 echo 版本 handle_echo()
│   └── history.py                  # 查詢歷史紀錄 API（尚未寫，晚點做）
│
├── models/                         # 資料庫相關（刻意先跳過，見下方「開發策略」）
├── db/                              # 資料庫連線（刻意先跳過）
│
├── static/                         # 前端靜態檔案
│   ├── index.html                  # 尚未寫（正式遊戲畫面）
│   └── game.js                     # 尚未寫
│
├── test_ws_server.py               # 已完成、已驗證能跑：最陽春的 WebSocket echo 測試伺服器（用 gevent.pywsgi，還沒用 gunicorn）
├── test_client.html                # 用瀏覽器測試 WebSocket 連線用的簡易頁面
│
├── requirements.txt                # 套件需求清單（尚未整理）
└── README.md
```

---

## 五、目前進度總結

**已經完成、驗證過的部分**：
1. 崩潰點機率公式 `game/crash.py`，理解長尾分佈原理
2. 倍率成長函式 `game/multiplier.py`
3. 單局遊戲邏輯 `game/room.py`（`run_game_round`），能正確算出崩潰點跟最終停止倍率
4. 環境驗證：`falcon`、`gevent`、`gevent-websocket` 套件都已安裝成功
5. 最陽春的 WebSocket echo 伺服器 `test_ws_server.py`，用 `gevent.pywsgi` + `WebSocketHandler` 手寫最原始的 WSGI 應用程式（還沒用 falcon 包裝），已經能啟動、監聽 8000 port
6. 正在測試：用瀏覽器 `test_client.html` 連線這個 echo 伺服器，驗證能不能正常收發訊息

**還沒開始的部分**：
- 把 `run_game_round` 的「一次跑完」邏輯，改寫成「逐步推送」版本，接上真正的 WebSocket（不是 echo，是真的遊戲邏輯）
- 用 falcon 正式定義路由（目前的 `test_ws_server.py` 是繞過 falcon 的手寫版本）
- 用 gunicorn 啟動（目前只是用 `pywsgi` 直接跑，還沒用正式的 `gunicorn --worker-class ...` 指令）
- 玩家下注、餘額扣款、結算邏輯（目前完全沒有「錢」的概念，只有純粹的機率跟倍率）
- `models/`、`db/`：資料庫相關，刻意延後（見下方策略）
- 前端正式畫面（目前只有純測試用的 `test_client.html`）
- 登入/註冊、歷史紀錄查詢

---

## 六、開發策略（重要，請遵循這個順序，不要跳著做）

**核心原則：先讓「遊戲玩起來」這件事最優先驗證，資料庫、玩家系統都是可以晚一點處理的外圍功能。**

```
1. [已完成] game/crash.py — 機率邏輯
2. [已完成] game/multiplier.py — 倍率成長
3. [已完成] game/room.py — 單局遊戲邏輯（一次跑完版本）
4. [已完成] 環境驗證：套件安裝、最陽春 WebSocket echo 能跑
5. [進行中] 用瀏覽器測試 echo 伺服器，確認連線、收發訊息正常
6. [下一步] 把 run_game_round 改寫成「逐步推送」版本：
   - 不是一次跑完迴圈才回傳結果
   - 而是每成長一步，就透過 ws.send() 把目前倍率推給前端
   - 同時要能監聽玩家傳來的 "cash_out" 訊息，隨時可能提前結束
7. 把上面這個邏輯，接進正式的 resources/game_ws.py（取代目前的 handle_echo）
8. 寫一個最簡單的 app.py，用 falcon 正式定義路由（取代 test_ws_server.py 的手寫版本）
9. 改用 gunicorn 啟動（--worker-class geventwebsocket.gunicorn.workers.GeventWebSocketWorker）
10. 前端：寫一個簡單的網頁，能顯示即時倍率、有「停止煉製」按鈕
11. 玩過幾輪，確認整個遊戲迴圈是通的、好玩的、機率合理
12. 這時候才回頭做「錢」的概念：用記憶體裡的 dict 假裝有玩家餘額（不用資料庫）
13. 確認下注、結算、扣款/加錢的邏輯沒問題
14. 這時候才回頭，把 dict 換成真正的資料庫（models/ + db/）
15. 最後才做登入註冊這種帳號系統（resources/auth.py）
```

**給接手的 AI 助手的提醒**：如果使用者要求「直接跳去做資料庫」或「直接做登入系統」，可以提醒他目前的開發策略是先把核心遊戲迴圈跑起來，資料庫是刻意延後的部分，除非使用者明確表示要改變順序。

---

## 七、已知的重要技術細節（避免重複踩坑）

1. **`gevent.sleep()` 不是 `time.sleep()`**：在 gevent 協程環境裡，一定要用 `gevent.sleep()`，這樣才會正確「讓出控制權」給其他協程處理，用一般的 `time.sleep()` 會讓整個 worker 卡住，其他玩家的連線都會被阻塞。

2. **`ws.receive(timeout=0)` 是非阻塞檢查**：用來在遊戲迴圈裡「順便」檢查玩家有沒有傳「停止」的訊息，不用 `timeout=0` 的話，`receive()` 會一直等待，卡住整個迴圈。

3. **Mutable 預設參數陷阱**：如果之後 `room.py` 裡要用 class 管理「一場遊戲房間」的狀態，記得避免 `def __init__(self, players=[])` 這種寫法，要用 `players=None` + 函式內部 `if players is None: players = []`。

4. **迴圈結束時的倍率細節**：`run_game_round` 回傳的 `current_multi`，是「超過」`crash_point` 的值，不是迴圈內最後一次的中間值，這才是正確的「爆炸倍率」。

5. **WebSocket 連線建立，本質上是 HTTP 升級**：如果之後除錯連線問題，記得檢查有沒有成功完成 `Upgrade: websocket` 的握手，這是 WebSocket 特有的一層，一般 HTTP 除錯流程（ping/telnet）沒有涵蓋到這一步。

---

## 八、學習脈絡（給接手的人參考，理解使用者已經具備哪些背景知識）

使用者已經完整學過（可以放心假設對方懂這些，不用重新解釋基礎）：
- Python 底層機制：GC、GIL、mutable/immutable、shallow/deep copy、scope、decorator
- Python 資料結構效率選擇（list/set/dict）、OOP（封裝/繼承/多型、classmethod/staticmethod）
- JSON 序列化、TCP/IP 四層、HTTP 協定完整細節（GET/POST/PUT/DELETE、keepalive、header）
- WSGI 介面意義、requests 套件用法
- WebSocket 原理（跟 HTTP 的升級關係）

使用者的學習風格：**偏好自己動手寫，卡住才給提示，不喜歡直接被餵完整答案**；容易在「完美主義的落差」卡住（想像中的成品跟實際能力有落差時容易卡住），適合用「填空、小步驟驗證」的方式引導。

---

## 待辦事項清單（給編輯器 AI 的具體任務）

- [ ] 確認 `test_client.html` 能成功連上 `test_ws_server.py`，收發 echo 訊息正常
- [ ] 把 `game/room.py` 的 `run_game_round` 改寫成 generator 或接受 `ws` 參數的逐步推送版本
- [ ] 在 `resources/game_ws.py` 寫一個新函式（例如 `handle_game_round(ws)`），整合機率邏輯 + WebSocket 推送 + 監聽玩家停止指令
- [ ] 寫 `app.py`，用 falcon 定義路由，把 `game_ws.py` 接進去
- [ ] 改用 `gunicorn --worker-class geventwebsocket.gunicorn.workers.GeventWebSocketWorker app:app` 啟動，取代 `test_ws_server.py`
- [ ] 寫一個正式的 `static/index.html` + `static/game.js`，顯示即時倍率動畫、下注輸入框、停止按鈕
- [ ] 加入記憶體版的玩家餘額（`fake_players` dict），串接下注扣款、結算加錢邏輯
- [ ] 整理 `requirements.txt`
