# 帳號與資產實作 — 學習筆記

## 階段 1：MongoDB 連線與資料層

### 連線方式
```python
from pymongo import MongoClient

client = MongoClient("mongodb://localhost:27017/")
db = client["game_db"]
```

### Unique Index
- `username` 欄位需要唯一性，防止重複註冊
- 設定方式：`db.players.create_index("username", unique=True)`
- 重複插入會觸發 `pymongo.errors.DuplicateKeyError`

### 玩家資料結構
一筆玩家資料包含：
- `username` — 帳號名稱（唯一）
- `password` — 密碼（目前明文，之後換 bcrypt）
- `balance` — 餘額（新玩家預設 1000.0）
- `created_at` — 建立時間（用 `datetime.utcnow()`，存 UTC 時間）

### pymongo 基本操作
- 插入：`db.players.insert_one({...})`
- 查詢：`db.players.find_one({"username": "xxx"})` → 找不到回傳 `None`

---

## 階段 2：最陽春的 HTTP 端點（falcon）

### falcon Resource 基本結構
- 接 POST 請求 → 方法叫 `on_post(self, req, resp)`
- 取得前端送來的 JSON → `req.get_media()`
- 回傳 JSON 給前端 → `resp.media = {"key": "value"}`
- 設定 HTTP 狀態碼 → `resp.status = '201 Created'`

### RegisterResource 邏輯
1. 從 request body 取出 `username`、`password`
2. 用 `db.players.insert_one(...)` 插入資料庫
3. 成功 → 回傳 201 + `{"message": "註冊成功"}`
4. 重複 username → 捕捉 `DuplicateKeyError`，回傳 409 + 錯誤訊息

### LoginResource 邏輯
1. 從 request body 取出 `username`、`password`
2. 用 `db.players.find_one({"username": username})` 查詢
3. 找不到 → 回傳 404 + `{"message": "使用者不存在"}`
4. 密碼不對 → 回傳 401 + `{"message": "密碼錯誤"}`
5. 密碼正確 → 回傳 200 + `{"message": "登入成功"}`

### 路由設定（app.py）
```python
from resources.auth import RegisterResource, LoginResource

falcon_app.add_route('/register', RegisterResource())
falcon_app.add_route('/login', LoginResource())
```

### HTTP 狀態碼速記
| 狀態碼 | 意思 | 用在哪 |
|--------|------|--------|
| 200 OK | 請求成功 | 登入成功 |
| 201 Created | 成功建立新資源 | 註冊成功 |
| 401 Unauthorized | 認證失敗 | 密碼錯誤 |
| 404 Not Found | 找不到 | 帳號不存在 |
| 409 Conflict | 資源衝突 | 帳號重複 |

### 測試方式（用 Python requests）
```python
import requests

# 註冊
r = requests.post('http://localhost:8000/register', json={'username': 'ayu', 'password': 'abc123'})
print(r.status_code, r.json())

# 登入
r = requests.post('http://localhost:8000/login', json={'username': 'ayu', 'password': 'abc123'})
print(r.status_code, r.json())
```
