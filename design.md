# design.md

> 版面配置參考：皇家強化爐（Royal Forge）demo
> 主題轉換：鍛造武器 → 煉製藥水；強化等級 → 倍率；碎裂 → 藥水爆炸
> 視覺風格：魔法感、紫色/邪惡綠配色、按鈕偏圓潤

---

## 一、整體版面配置（雙欄式）

```
┌─────────────────────────┬───────────────────────────────────┐
│                          │                                     │
│  左欄：資訊/操作面板         │  右欄：煉藥舞台（視覺主場）              │
│  （約佔畫面寬度 25%）        │  （約佔畫面寬度 75%）                  │
│                          │                                     │
│  固定寬度、深色背景          │  暗色調背景 + 細格紋理                  │
│  由上到下垂直排列資訊卡片      │  中央呈現藥水釜/魔法陣動畫              │
│                          │                                     │
│                          │                                     │
├─────────────────────────┤                                     │
│  底部：兩個按鈕橫排           │                                     │
│  （賣出/停止 + 主要動作）      │                                     │
└─────────────────────────┴───────────────────────────────────┘
```

**左欄由上到下的區塊順序**（對照參考截圖）：

1. **標題區**：小型大寫英文標籤（如 `MAGIC GRANDMA`）+ 中文主標題（如「魔法阿嬤」）
2. **金幣顯示**（右上角獨立小卡片）：金幣圖示 + 「持有金幣」標籤 + 大字數字
3. **煉藥資訊卡片**：藥水圖示 + 配方名稱 + 一行說明文字
4. **數值列表**（每行左標籤右數值，用細分隔線隔開）：
   - 起始倍率 / 目前倍率
   - 下注金額 / 本輪總投入
   - 成功率 / 下次成功率
5. **風險警告文字**（醒目色，通常是暖色系警示，這裡建議用亮紫或洋紅提醒）
6. **投入金額調整區**：金額顯示 + 加號按鈕（圓形）
7. **價值變化提示**：目前可賣價 → 停止後可得金額（用箭頭連接）
8. **輔助說明文字**（小字、次要色）
9. **底部雙按鈕**：左側次要按鈕（賣出/停止）、右側主要按鈕（下注/繼續煉製，佔比較大、顏色最醒目）

**右欄（煉藥舞台）**：

- 背景：深色，帶淡淡格紋或魔法陣紋理，四個角落有科幻風格的 HUD 邊角裝飾線
- 左上角：小型狀態文字（如 `MAGIC CORE ■ STANDBY` / `MAGIC CORE ■ BREWING`）
- 右上角：倍率徽章（藥水投入前不顯示，投入後浮現，顯示目前倍率如「1.32x」）
- 中央：主視覺
  - 未下注時：空的魔法陣/煉藥鍋外框（菱形或圓形圖騰），文字「空的煉藥台」+「投入金幣以啟動」
  - 煉製中：藥水瓶/藥水鍋，隨倍率上升有光暈脈動、顏色漸變動畫，外框發光
- 底部：煉藥鍋/工作台插畫 + 一條進度條（代表煉製節奏或風險累積）

---

## 二、色彩系統

### 核心配色：魔法紫 + 邪惡綠，深色基底

```css
:root {
  /* 背景 */
  --bg-primary: #16121F;        /* 主背景，深紫黑 */
  --bg-panel: #1F1830;          /* 左側面板背景，比主背景略亮 */
  --bg-stage: #100D19;          /* 右側舞台背景，更深，突顯中央視覺 */
  --bg-card: #2A2140;           /* 資訊卡片背景 */

  /* 邊框 */
  --border-default: #3D3158;    /* 一般邊框 */
  --border-glow: #8B5CF6;       /* 發光邊框（強調用） */

  /* 主色：魔法紫 */
  --purple-500: #8B5CF6;        /* 主紫色，用於次要強調 */
  --purple-400: #A78BFA;        /* 亮紫，用於 hover 狀態 */
  --purple-700: #5B3AA8;        /* 深紫，用於陰影/次要背景 */

  /* 主色：邪惡綠（毒藥感） */
  --venom-green: #6FE05C;       /* 主要成功/正向色，帶毒液感的螢光綠 */
  --venom-green-dim: #2F5E28;   /* 深綠，用於底色/次要狀態 */
  --venom-glow: #9AFF7A;        /* 高亮發光綠，用於粒子特效、成功動畫 */

  /* 警示/危險 */
  --danger: #FF4D6A;            /* 爆炸/失敗警示，玫瑰紅 */
  --warning-text: #E39CFF;      /* 風險提示文字，淡紫粉 */

  /* 文字 */
  --text-primary: #F0EAFF;      /* 主要文字，帶一點紫的白 */
  --text-secondary: #B8ACD9;    /* 次要文字 */
  --text-muted: #6E6291;        /* 輔助/淡化文字 */

  /* 金幣/貨幣 */
  --gold: #F5C518;              /* 金幣顏色維持金黃，與紫綠形成對比 */
}
```

### 配色使用原則

- **主背景用深紫黑（`--bg-primary`），不要用純黑**——保留一點紫色調性，才有魔法氛圍，避免看起來只是普通的深色模式
- **邪惡綠只用在「正向、成功、進行中」的狀態**：倍率數字上升時的顏色、煉製中的光暈、成功結算的提示
- **紫色是主結構色**：面板背景、邊框、卡片、次要按鈕
- **金色只用在金幣/貨幣相關數字**，維持跟「錢」的視覺連結，不跟主色調混淆
- **危險色（玫瑰紅）只在爆炸/失敗瞬間出現**，平常不常駐畫面，讓它出現時才有衝擊力

### 倍率數字的漸層變化（隨風險升高）

```
1.00x ~ 2.00x   → 邪惡綠（--venom-green），代表安全區
2.00x ~ 5.00x   → 綠轉紫漸變，代表風險升高
5.00x 以上        → 紫轉洋紅（--warning-text 到 --danger 之間漸變），代表高風險、隨時可能爆炸
```

---

## 三、按鈕設計（偏圓潤）

### 基本規格

```css
.btn {
  border-radius: 16px;           /* 明顯圓角，比參考截圖的直角風格更柔和 */
  padding: 14px 28px;
  font-weight: 600;
  font-size: 15px;
  border: none;
  cursor: pointer;
  transition: transform 0.12s ease, box-shadow 0.12s ease;
}

.btn:hover {
  transform: translateY(-1px);
}

.btn:active {
  transform: translateY(0px) scale(0.98);
}
```

### 主要按鈕（下注/繼續煉製）

```css
.btn-primary {
  background: linear-gradient(135deg, #8B5CF6, #6FE05C);
  /* 紫綠漸層，是整個介面最搶眼的元素 */
  color: #16121F;
  border-radius: 20px;            /* 主按鈕圓角更大，強調「這是最重要的操作」 */
  box-shadow: 0 4px 20px rgba(139, 92, 246, 0.4);
}

.btn-primary:hover {
  box-shadow: 0 6px 24px rgba(139, 92, 246, 0.6);
}
```

### 次要按鈕（賣出/停止煉製）

```css
.btn-secondary {
  background: var(--bg-card);
  color: var(--text-primary);
  border: 1.5px solid var(--border-default);
  border-radius: 16px;
}

.btn-secondary:hover {
  border-color: var(--purple-400);
}
```

### 危險/確認類按鈕（例如確定放棄煉製）

```css
.btn-danger {
  background: var(--danger);
  color: #16121F;
  border-radius: 16px;
}
```

### 圓形小按鈕（金額加減、關閉視窗）

```css
.btn-round {
  width: 40px;
  height: 40px;
  border-radius: 50%;             /* 完全圓形，用在加減金額這類輔助操作 */
  background: var(--bg-card);
  border: 1px solid var(--border-default);
}
```

---

## 四、卡片與資訊區塊

```css
.info-card {
  background: var(--bg-card);
  border: 1px solid var(--border-default);
  border-radius: 18px;             /* 卡片圓角統一比按鈕略小，維持層次感 */
  padding: 16px 20px;
}

.stat-row {
  display: flex;
  justify-content: space-between;
  padding: 10px 0;
  border-bottom: 1px solid var(--border-default);
  font-size: 14px;
}

.stat-row .label { color: var(--text-secondary); }
.stat-row .value { color: var(--text-primary); font-weight: 600; }
```

---

## 五、字體與排版

```css
--font-display: 'Cinzel', serif;        /* 標題用略帶魔法/中世紀感的襯線字體（如果支援中文，可改用書法感較重的黑體變體）*/
--font-body: -apple-system, 'Noto Sans TC', sans-serif;   /* 內文維持清楚易讀的無襯線體 */
--font-mono: 'JetBrains Mono', monospace;  /* 數值、狀態標籤（如 MAGIC CORE ■ BREWING）用等寬字體，呼應參考截圖的科技感標籤風格 */
```

- **主標題**（如「巫婆煉藥鍋」）：24px，字重 700
- **數值/倍率顯示**：舞台中央的倍率數字要大，建議 48-64px，等寬字體，數字跳動時有輕微縮放動畫
- **標籤文字**（如 `MAGIC CORE ■ STANDBY`）：11px，等寬字體，字距加寬（`letter-spacing: 0.08em`），全大寫

---

## 六、舞台中央視覺（右欄核心）

### 狀態一：未下注（Empty）

- 中央顯示一個空的魔法陣輪廓（菱形或圓形圖騰，虛線或半透明描邊，紫色調）
- 文字：「空的煉藥台」+ 次要文字「投入金幣以啟動核心」
- 底部煉藥鍋插畫呈現「熄火」狀態（灰暗、無光）

### 狀態二：煉製中（Brewing）

- 藥水瓶/煉藥鍋主視覺，外框發光（顏色隨倍率從綠漸變到紫到洋紅，對應第二節的漸層規則）
- 右上角倍率徽章持續跳動更新
- 底部煉藥鍋插畫「點火」，有粒子/氣泡上升的動畫效果（邪惡綠色小光點，象徵魔法能量）
- 進度條或波動線，呈現煉製節奏的視覺回饋

### 狀態三：成功停止（Cashed Out）

- 短暫的邪惡綠色閃光/粒子爆發動畫
- 藥水瓶呈現「完成」的飽滿發光狀態
- 顯示最終倍率與獲得金額

### 狀態四：爆炸（Crashed）

- 危險色（玫瑰紅）閃光，畫面短暫震動（screen shake）
- 藥水瓶碎裂的視覺效果（可用簡單的裂紋 + 淡出動畫）
- 文字提示「藥水爆炸了」+ 顯示崩潰倍率

---

## 七、與參考截圖的對照表

| 皇家強化爐（參考） | 巫婆煉藥鍋（你的版本） |
|---|---|
| 武器（熾鐵長劍） | 藥水配方（例如「初階回復藥」） |
| 強化等級 +0, +1... | 倍率 1.00x, 1.32x... |
| 武器買價 | 下注金額 |
| 每次強化費 | （你的遊戲不需要，是持續下注，不是分次付費強化） |
| 首次成功率 / 下次成功率 | 崩潰機率提示（可選擇是否顯示給玩家，多數 Crash Game 不會即時顯示精確機率） |
| 賣出裝備 | 停止煉製（結算） |
| 購買武器 | 開始煉製（下注） |
| 目前賣價 → 成功後賣價 | 目前可得金額（即時計算：下注金額 × 目前倍率） |
| 碎裂警告文字 | 爆炸風險警告文字 |
| PIXEL CORE ■ STANDBY/FORGED | MAGIC CORE ■ STANDBY/BREWING |
| 金橘色主色調 | 紫色 + 邪惡綠雙主色 |
| 直角、硬朗的按鈕 | 圓潤、柔和的按鈕（本文件的重點調整） |

---

## 八、與你的技術架構對照

這份視覺設計對應到你專案架構裡的：

```
static/
├── index.html      # 對應本文件的整體版面結構
├── style.css        # 對應第二、三、四、五節的色彩/元件規範
└── game.js            # 負責：
                          # - 接收 WebSocket 推送的 multiplier，更新中央倍率顯示與漸層顏色
                          # - 監聽「開始煉製」「停止煉製」按鈕，對應送出 WebSocket 訊息
                          # - 播放對應狀態動畫（Empty / Brewing / Cashed Out / Crashed）
```

---

## 待釐清 / 待深入
- [ ] 藥水配方的視覺素材：需要至少一組「藥水瓶」美術資源（未煉製/煉製中/完成/爆炸四種狀態）
- [ ] 動畫細節：粒子效果、screen shake 具體用什麼前端技術實作（CSS animation 足夠，還是需要 Canvas/WebGL）
- [ ] 響應式設計：手機版的雙欄配置怎麼改（可能改成上下堆疊，舞台在上、操作面板在下）
